In an application, the version and other configuration parameters can be defined using YML files.
These settings can also be configured or overridden through
command-line arguments
environment variables
JVM system properties
profile-specific configuration files
external configuration files
Docker or Kubernetes environment configurations
Spring Cloud Config Server.